<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="15dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/img_main_category"
        android:layout_width="120dp"
        android:layout_height="80dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"
        />
    <TextView
        android:id="@+id/txt_main_category"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/img_main_category"
        app:layout_constraintBottom_toBottomOf="@id/img_main_category"
        app:layout_constraintLeft_toRightOf="@id/img_main_category"
        app:layout_constraintRight_toLeftOf="@id/btn_drop_down_main"
        android:paddingHorizontal="20dp"
        android:text="Organik"
        android:textSize="18sp"
        android:textColor="@color/main_category"
        />
    <ImageButton
        android:id="@+id/btn_drop_down_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/img_main_category"
        app:layout_constraintBottom_toBottomOf="@id/img_main_category"
        app:layout_constraintRight_toRightOf="parent"
        android:background="@drawable/ic_expand_more_32"
        >

    </ImageButton>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_main_category"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/img_main_category"
        android:visibility="gone"
        >

    </androidx.recyclerview.widget.RecyclerView>
</androidx.constraintlayout.widget.ConstraintLayout>